# Optional: set only when the built frontend must call a remote API (e.g. Render).
# Leave unset for local dev — src/config.js uses http://127.0.0.1:5000 automatically in development.
#
# REACT_APP_API_URL=https://wfa-xgb-cvd-app.onrender.com
